# 数据表设计方案

## 设计思路

### 部门表（department）

四级结构采用**邻接表（Adjacency List）**模式，每条记录保存 `parent_id` 指向父部门，`level` 字段明确标注所在层级（1=公司, 2=一级部门, 3=二级部门, 4=三级部门），`parent_id` 为 NULL 表示顶层（公司），叶节点无子部门但自身记录存在（子部门是否存在通过查子记录判断，无需额外 null 标记）。

```sql
CREATE TABLE department (
  id          INT UNSIGNED    NOT NULL AUTO_INCREMENT,
  name        VARCHAR(100)    NOT NULL COMMENT '部门名称',
  level       TINYINT         NOT NULL COMMENT '层级：1=公司 2=一级部门 3=二级部门 4=三级部门',
  parent_id   INT UNSIGNED    NULL      COMMENT '父部门ID，NULL表示顶层公司',
  sort_order  SMALLINT        NOT NULL DEFAULT 0 COMMENT '同级排序号',
  created_at  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  FOREIGN KEY (parent_id) REFERENCES department(id) ON DELETE RESTRICT
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='部门表';
```

**设计说明**：
- `level` + `parent_id` 双重保障层级关系清晰，避免递归查询时迷失层级
- `parent_id` 自引用外键，防止非法父部门 ID
- `ON DELETE RESTRICT`：有子部门时禁止删除父部门，保护数据完整性

---

### 人员表（person）

```sql
CREATE TABLE person (
  id           INT UNSIGNED    NOT NULL AUTO_INCREMENT,
  username     VARCHAR(50)     NOT NULL UNIQUE      COMMENT '登录用户名',
  password     VARCHAR(100)    NOT NULL             COMMENT '登录密码（明文）',
  real_name    VARCHAR(50)     NOT NULL             COMMENT '真实姓名',
  class        INT             NOT NULL DEFAULT 9   COMMENT '用户级别：数字越小级别越高，0=admin，1=总经理，2=一级部门经理，依此类推',
  job          VARCHAR(100)    NOT NULL             COMMENT '工作岗位',
  dept_id      INT UNSIGNED    NOT NULL             COMMENT '所属部门ID（关联最细粒度部门）',
  phone        VARCHAR(20)     NULL                 COMMENT '手机号',
  email        VARCHAR(100)    NULL                 COMMENT '电子邮箱',
  status       TINYINT         NOT NULL DEFAULT 1   COMMENT '状态：1=在职 0=离职',
  created_at   DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at   DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  FOREIGN KEY (dept_id) REFERENCES department(id) ON DELETE RESTRICT,
  CONSTRAINT chk_phone  CHECK (phone  IS NULL OR phone  REGEXP '^1[3-9][0-9]{9}$'),
  CONSTRAINT chk_email  CHECK (email  IS NULL OR email  REGEXP '^[A-Za-z0-9._%+\\-]+@[A-Za-z0-9.\\-]+\\.[A-Za-z]{2,}$')
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='人员表';
```

**设计说明**：
- `dept_id` 外键指向 department 表，人员挂靠到最细粒度部门，层级关系通过 department.parent_id 链路追溯
- `class` 为 INT 类型，数字越小级别越高：0=admin，1=总经理，2=一级部门经理，依此类推
- `password` 存明文
- `CHECK` 约束：phone 为国内手机号格式（1开头11位），email 为标准格式；值为 NULL 时约束不触发

---

### 层级关系说明

通过 `department.parent_id` 自引用实现完整四级树：

```
公司（level=1, parent_id=NULL）
 ├── 市场部（level=2, parent_id=公司ID）
 │    ├── 华东市场部（level=3, parent_id=市场部ID）
 │    │    └── 上海小组（level=4, parent_id=华东市场部ID）
 ├── 设计部（level=2, parent_id=公司ID）
 │    └── UI设计组（level=3, parent_id=设计部ID）
```

---

## 虚拟数据设计

**部门数据（覆盖四级）**：

| level | 名称 | parent |
|-------|------|--------|
| 1 | 某某科技有限公司 | NULL |
| 2 | 市场部 | 公司 |
| 2 | 设计部 | 公司 |
| 2 | 技术部 | 公司 |
| 2 | 行政部 | 公司 |
| 3 | 华东市场组 | 市场部 |
| 3 | 华北市场组 | 市场部 |
| 3 | UI设计组 | 设计部 |
| 3 | 前端开发组 | 技术部 |
| 4 | 上海小组 | 华东市场组 |

**人员数据**：

| username | class | job | dept |
|----------|-------|-----|------|
| admin | 0（系统admin）| 系统管理员 | 某某科技有限公司 |
| zhangjian | 1（总经理）| 总经理 | 某某科技有限公司 |
| lihua | 2（一级部门经理）| 市场部经理 | 市场部 |
| wangfang | 2（一级部门经理）| 设计部经理 | 设计部 |
| zhangsan | 3（二级部门主管）| 华东市场组主管 | 华东市场组 |
| lisi | 4（普通员工）| UI设计师 | UI设计组 |
| wangwu | 4（普通员工）| 前端工程师 | 前端开发组 |

---

## 执行文件规划

将创建以下 SQL 脚本文件：
- `sql/01_create_db.sql`：创建数据库 db02
- `sql/02_create_tables.sql`：创建 department、person 表
- `sql/03_insert_test_data.sql`：插入虚拟测试数据

Node.js 执行入口（后续任务使用）：连接参数读取自 `.env` 文件。
